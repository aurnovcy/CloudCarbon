# CloudCarbon Web App

This directory is a placeholder. The React + Vite frontend is managed by **Lovable**.

The frontend consumes types from `@cloudcarbon/api-types` and communicates exclusively
through the REST API defined in `docs/openapi.yaml`.
